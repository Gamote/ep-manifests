{
  "installationSteps": [
    "/bin/bash -c '$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)'"
  ],
  "checkHealthCommand": "brew --version",
  "installCommandTemplate": "brew install {name}@{version}"
}
